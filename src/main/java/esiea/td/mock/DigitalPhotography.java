package esiea.td.mock;

public class DigitalPhotography{
    static void Main(String[] args)
    {
        /*
        DigitalPhotographer digitalPhotographer = new DigitalPhotographer();
        IDigitalCamera digitalCamera = new DigitalCameraReal();
        digitalPhotographer.SetDigitalCamera(digitalCamera);
        Image image = (Image) digitalPhotographer.TakePictureWithFlash();
        try
        {
            image.Save("output.jpg");
        }
        catch (IOException ioException)
        {
            Console.WriteLine(ioException.StackTrace);
        }
        */
    }
}